# Server-side backend API base URL (used by Next.js proxy route)
BACKEND_API_BASE=http://127.0.0.1:8000

# Server-side backend bearer token (never expose with NEXT_PUBLIC_ prefix)
BACKEND_API_KEY=your_backend_bearer_token
