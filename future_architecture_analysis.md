# HK-FinReg AI: 2026 架构升级与演进分析报告（修订版）

结合截至 2026 年 4 月 29 日的 AI 工程化、Agent 编排、RAG / GraphRAG、模型评估与 RegTech 最新实践，对 HK-FinReg AI 当前架构进行再评估，并给出更贴近生产落地的下一阶段升级路线。

---

## 摘要

HK-FinReg AI 当前架构已经具备 2026 年语境下较强的基础竞争力：多智能体反思循环、Hybrid RAG、三维置信度、流式可观测性、结构化输出校验，这些能力说明项目并不落后，反而已经越过了“Demo 级问答系统”的阶段，开始向**可解释、可追踪、可恢复的合规 Agent 系统**演进。

但如果以 2026 年金融合规和高风险 AI 的要求作为标尺，项目下一步的重点不应只是“再引入更潮的 Agent / Graph / Protocol 名词”，而应优先补齐四类真正决定可落地性的能力：

1. **工作流级持久化与 Human-in-the-Loop**
2. **证据血缘、法规版本化与审计日志**
3. **离线评估门禁与回归数据集**
4. **工具接入安全、检索污染防护与模型治理**

结论上，原始升级攻略的大方向是正确的，但优先级需要调整：

- `PostgresSaver + interrupt + 审计日志` 应从“一个提升点”上升为**第一优先级**
- `RAGAS` 应保留，但要从“单框架评估”升级为**RAGAS + gold set + claim-level provenance** 的复合评估体系
- `MCP` 适合进入中期落地
- `GraphRAG` 适合做高价值子域试点，不适合直接替换全部 Hybrid RAG
- `A2A` 应被视为中长期的跨系统演进方向，而不是近期主轴

---

## 一、当前架构在 2026 年是否仍然新颖

### 1.1 仍然具备竞争力的部分

HK-FinReg AI 当前已有设计，放到 2026 年依旧不显老：

- **LangGraph 反思循环**：相比单轮 LLM 调用，已经明显更接近企业级 Agent 工作流
- **Hybrid RAG（Dense + BM25 + RRF + Reranker）**：对法规、条款编号、专有名词密集的 RegTech 场景仍然很实用
- **结构化输出 + Validator + Reviewer**：对合规场景尤其重要，这一点比单纯提高模型参数更有价值
- **三维置信度**：虽然仍需增强，但方向正确，符合 2026 年“模型输出必须伴随质量信号”的趋势
- **流式可观测性**：对长链路 Agent 审查过程非常关键，尤其适合合规官审阅与调试

### 1.2 不再算“前沿”的部分

以下内容在 2026 年依然有用，但已经不属于“仅靠它就足够先进”的范畴：

- 单纯的 `Hybrid RAG`
- 单纯的 `LLM-as-a-judge`
- 仅有最终答案溯源、没有中间步骤血缘追踪
- 仅有内存状态的 LangGraph 工作流

换言之，项目当前技术栈**不是落后**，但已经来到必须从“模型功能”走向“治理能力”的阶段。

---

## 二、2026 年真正高优先级的补强方向

## 2.1 生产级状态持久化与真正的 HITL

### 判断

这是最应该优先落地的升级项。

在高风险合规场景里，系统是否能暂停、恢复、等待人工批注、完整记录责任链，往往比“检索是否多 3 个点召回率”更重要。2026 年的监管环境对这一点已经非常明确：高风险 AI 决策要有**人类监督、日志记录、可追溯恢复**。

### 为什么优先

- 当前 LangGraph 仍以短生命周期执行为主，重启或超长任务恢复能力有限
- 当前 Reviewer 虽能拒绝报告，但对“人工接管”的编排还不够完整
- 在 RegTech 场景里，真正的责任归属不在模型，而在“谁在什么证据下批准了什么结论”

### 修订后的升级建议

1. 将 `PostgresSaver` 从优化项提升为**基础设施项**
2. 为每个工作流实例保存以下对象：
   - 状态快照
   - 检索证据集
   - 中间草稿版本
   - reviewer 判决
   - 人工介入记录
   - 最终批准人和时间戳
3. 将 `interrupt()` 机制设计为三种标准暂停点：
   - `low_confidence_gate`
   - `missing_evidence_gate`
   - `manual_approval_gate`
4. 前端不只展示 `[Action Required]`，还要支持：
   - 查看当前证据链
   - 补充外部事实
   - 驳回建议并重跑
   - 记录人工批注与恢复原因

### 结论

这一项不是“锦上添花”，而是系统进入生产环境前的**硬门槛**。

---

## 2.2 证据血缘、法规版本化与审计日志

### 判断

这是原文档最需要补齐、但着墨不足的一层。

2026 年 RegTech 系统最怕的不是“答不出来”，而是：

- 引用了过期法规
- 混用了不同司法辖区的规则
- 使用了无法解释来源的中间判断
- 无法重建当时的证据上下文

### 为什么它比单纯 GraphRAG 更急

GraphRAG 解决的是“关系表示能力”；而法规版本化、审计日志、证据血缘解决的是“监管可采信能力”。对合规系统来说，后者优先级更高。

### 新增升级建议

1. 为每个法规片段补齐治理元数据：
   - `jurisdiction`
   - `effective_date`
   - `last_updated_date`
   - `supersedes`
   - `superseded_by`
   - `document_version`
   - `regulatory_topic`
2. 将当前 `[Source: N, p.X]` 扩展为两层溯源：
   - 文本级溯源：引用到具体 chunk / page
   - 工作流级溯源：记录该引用由哪一次检索、哪一个节点、哪一轮修订引入
3. 为每份最终报告生成不可变审计包：
   - 输入摘要
   - 证据快照
   - 模型版本
   - Prompt / policy 版本
   - 审批日志
   - 置信度与告警历史
4. 将“法规更新触发回归测试”纳入流程：
   - 新法规入库
   - 标记受影响 query set
   - 自动重跑关键场景

### 结论

如果不先补这层，后续无论接不接 GraphRAG、A2A，系统都还称不上真正的生产级 RegTech 平台。

---

## 2.3 RAGAS 评估体系应升级为复合评估体系

### 判断

原文档对 `RAGAS` 的方向判断是正确的，但表述偏乐观。

RAGAS 在 2026 年仍然是有价值的基础工具，但它不应被视为“无需 Ground Truth 即可全面评价 RegTech 系统”的充分条件。尤其在合规场景中，仅靠 LLM-as-a-judge 风险较高。

### 原方案的问题

- `Context Recall` 往往仍依赖 reference / gold answer
- `Faithfulness` 能发现一部分幻觉，但不等于法规级正确性
- 纯自动生成测试集容易把模型偏差重新注入评估集

### 修订后的升级建议

将评估体系分为三层：

1. **自动评估层**
   - `RAGAS Context Precision`
   - `RAGAS Faithfulness`
   - `Answer Relevance`
   - latency / token / retrieval hit-rate
2. **规则评估层**
   - 引用格式完整率
   - 是否引用失效法规
   - 是否跨 jurisdiction 混用条款
   - 是否存在无来源结论
3. **人工标注 / gold set 层**
   - 20-50 个高价值监管场景的固定回归集
   - 每个场景包含应命中的法规集合
   - 每个场景包含不可接受错误类型

### 建议新增的指标

- `claim_coverage`
- `citation_validity`
- `version_correctness`
- `jurisdiction_consistency`
- `workflow_provenance_completeness`

### 结论

`RAGAS` 仍然值得做，但必须降级为“评估底座之一”，而不是唯一方案。

---

## 2.4 MCP 值得做，但应先做只读、强治理的工具层

### 判断

`MCP` 在 2026 年是有时效性的，尤其适合作为外部事实源和内部工具的标准接入层。原文档这一判断基本成立。

### 需要修正的地方

真正的风险不在“能不能接”，而在“接进来以后权限怎么管、日志怎么留、结果怎么验证”。RegTech 中工具调用比普通问答更敏感。

### 修订后的升级建议

1. 第一阶段只接**只读工具**
   - HKMA / SFC / FATF 公开资料
   - 内部 KYC 只读查询
   - 内部名单库只读校验
2. 每个 MCP 工具必须具备：
   - 明确权限边界
   - 参数白名单
   - 调用日志
   - 超时与失败回退
   - 工具输出可信度标记
3. 对工具结果做二次治理：
   - 区分“法规原文事实”与“外部业务数据”
   - 工具输出不可直接写入最终报告，必须经过 validator / reviewer 链
4. 工具注册应使用 capability 标签：
   - `regulatory_lookup`
   - `watchlist_check`
   - `kyc_lookup`
   - `document_version_lookup`

### 结论

MCP 应进入中期路线，但要作为**受治理的工具接入层**来做，而不是“更多工具调用能力”。

---

## 三、GraphRAG 的位置：值得做，但不应被表述为默认标准

### 判断

原文档把 GraphRAG 描述为“2025-2026 金融与法律领域的标准”，这个说法过满。更准确的判断是：

- GraphRAG 对**复杂法规关系、多跳影响分析、跨章节依赖推理**非常有价值
- 但在大量高频场景中，`Hybrid RAG + 结构化 metadata + reranker` 仍然是成本更低、稳定性更高的主力方案

### 什么问题适合 GraphRAG

- “新规如何影响既有 AML / CDD 流程”
- “某条指引和某牌照义务之间有哪些前置依赖”
- “跨文档修订关系和覆盖关系”
- “同一监管主题在多份指引中的约束链”

### 什么问题不必急着 GraphRAG

- 精确条款定位
- 法规编号命中
- 短上下文义务解释
- 单文档局部问答

### 修订后的升级建议

1. 不全量替换现有 `HybridRetriever`
2. 先为 `SVF / AML / CDD` 领域做一个 GraphRAG 试点
3. 将 Graph 检索设计成**按查询路由触发**
   - `specific_clause` / `entity_lookup` 继续走 Hybrid RAG
   - `impact_analysis` / `cross_reference_reasoning` 才触发 graph retrieval
4. 图谱建设先从低风险关系开始：
   - `references`
   - `amends`
   - `supersedes`
   - `applies_to`
   - `requires`

### 结论

GraphRAG 是值得投资的中长期能力，但更适合作为**高复杂度查询的增强层**，不是近期一刀切的底层替换。

---

## 四、A2A 是中长期方向，不应挤占近期交付资源

### 判断

原文档关于 `A2A` 的前瞻性没有问题，但优先级偏高。

2026 年 A2A 的意义主要体现在：

- 不同团队拥有不同 Agent 服务
- 多系统之间需要远程委托任务
- 组织级 agent mesh 开始成形

而目前 HK-FinReg AI 仍以单仓、单后端、单 LangGraph 工作流为主。在这个阶段，A2A 不是最紧迫的瓶颈。

### 更合理的升级顺序

1. 先把当前单体 LangGraph 拓扑抽象为：
   - 可配置节点注册
   - 能力标签
   - 工具 / 数据源解耦
2. 再做跨模块内部协同：
   - SVF
   - 银行开户
   - 跨境汇款
   - SME 信贷
3. 最后才是 A2A 化：
   - 远程发现
   - 任务委托
   - 状态同步
   - 协议兼容

### 修订后的建议

- 近期不把 A2A 作为核心交付里程碑
- 近期只在架构上预留：
  - agent capability schema
  - task envelope
  - standard review payload
  - resumable workflow ID

### 结论

A2A 应保留在路线图里，但明确标为**长期演进方向**。

---

## 五、2026 年必须新增的安全与治理能力

这一部分是原文档最缺的内容，建议新增为独立章节。

## 5.1 Prompt Injection 与检索污染防护

需要新增：

- 检索文本中的指令污染剥离
- 工具输出与法规文本隔离注入
- 用户输入、检索内容、工具结果三类信任域分离
- “模型不得遵循检索内容中的操作指令”作为系统级 policy

## 5.2 模型与工具权限治理

需要新增：

- 工具按读写等级划分
- 默认拒绝写操作
- 高风险工具必须人工批准
- 每次工具调用记录调用原因、调用参数和调用结果摘要

## 5.3 法规时效性治理

需要新增：

- 文档入库时间
- 失效法规自动标记
- 新旧版本映射
- 报告中显示法规生效日期与版本

## 5.4 监管级日志与保留策略

需要新增：

- 日志保留期限
- 审批链不可篡改记录
- 风险场景回放能力
- 针对关键结论的“事后追责可重建性”

---

## 六、修订后的演进路线建议

## 短期（0-3 个月）

目标：先把系统从“可运行”推进到“可审计、可恢复、可评估”。

1. 接入 `PostgresSaver`，实现工作流持久化与恢复
2. 落地 `interrupt()` 驱动的 HITL 断点
3. 补齐法规版本元数据和审计日志结构
4. 建立第一版评估门禁：
   - RAGAS
   - 引用有效性规则检查
   - 小规模 gold set
5. 建立工具接入安全策略，为后续 MCP 做准备

## 中期（3-6 个月）

目标：让系统具备可治理的外部事实接入和更完整的证据血缘。

1. 接入只读 MCP 工具
2. 引入 claim-level provenance 和 workflow-level lineage
3. 把法规变更触发回归测试纳入 CI
4. 为高风险场景增加人工审批模板与审计包导出能力
5. 将多业务模块统一到共享的 workflow policy 层

## 中长期（6-12 个月）

目标：在高复杂度场景上提升推理能力，而不是盲目扩大系统复杂度。

1. 在 SVF / AML / CDD 领域试点 GraphRAG
2. 为复杂影响分析查询增加 graph retrieval 路由
3. 评估是否需要将部分 agent 服务拆分成独立能力服务
4. 仅在跨系统协作需求明确后，再推进 A2A

---

## 七、最终结论

HK-FinReg AI 的下一阶段升级方向仍然是正确的，但 2026 年真正决定系统先进性的，不再只是“是否用了 GraphRAG / MCP / A2A”，而是是否具备以下四种能力：

1. **可中断、可恢复、可人工接管**
2. **可证明证据血缘与法规版本正确性**
3. **可通过自动与人工结合的评估门禁持续回归**
4. **可治理工具接入、安全边界与审计责任**

因此，本修订版建议将原路线图重排为：

`持久化/HITL/审计` → `评估与治理` → `MCP` → `GraphRAG` → `A2A`

这个顺序更贴近 2026 年 AI 与 RegTech 的真实落地节奏，也更适合 HK-FinReg AI 当前代码基础和产品阶段。
